import 'package:flutter/material.dart';

class PowerFingerGame extends StatelessWidget {
  static final String id = 'power_finger_game';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Power Finger'),
      ),
    );
  }
}
