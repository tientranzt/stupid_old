import 'package:flutter/material.dart';

class PlayAnimalGame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Play game'),
      ),
    );
  }
}
