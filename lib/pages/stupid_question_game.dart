import 'package:flutter/material.dart';

class StupidQuestionGame extends StatelessWidget {
  static final String id = 'stupid_question_game';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Stupid Question'),),
    );
  }
}
