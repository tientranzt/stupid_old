import 'package:flutter/material.dart';

class CountSheepGame extends StatelessWidget {
  static final String id = 'count_sheep_game';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Count sheep'),),
    );
  }
}
