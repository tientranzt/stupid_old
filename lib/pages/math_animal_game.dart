import 'package:flutter/material.dart';


class AnimalMathGame extends StatelessWidget {
  static final String id = 'animal_math_game';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Animal Math'),),
    );
  }
}
